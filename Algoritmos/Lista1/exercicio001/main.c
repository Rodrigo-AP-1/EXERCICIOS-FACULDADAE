#include <stdio.h>

int main() {
    int b, a, area;

    printf("qual a base do retangulo?:\n ");
    scanf("%d", &b);
    printf("qual a altura do retangulo?:\n ");
    scanf("%d", &a);

    area = b*a;

    printf("area do retangulo: %d ", area);


    
}